<UserControl x:Class="De.SealingSchool.View.StudentListView2"
             xmlns:my="clr-namespace:De.SealingSchool.View" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="LightGray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding Students}" DisplayMemberPath="Label" Name="studentList"/>
        <Button Command="{Binding AddStudentCommand}" Content="Add Student" Grid.Row="1"/>
        <my:StudentView Grid.Column="1" DataContext="{Binding ElementName=studentList, Path=SelectedItem}" HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</UserControl>
