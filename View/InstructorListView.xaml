<UserControl x:Class="De.SealingSchool.View.InstructorListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding Instructors}"  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Label" Binding="{Binding Label}" />
                <DataGridTextColumn Header="Vorname" Binding="{Binding FirstName}" />
                <DataGridTextColumn Header="Nachname" Binding="{Binding LastName}" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Kursleiter hinzufügen" Command="{Binding AddInstructorCommand}" Grid.Row="1" />
    </Grid>
</UserControl>
